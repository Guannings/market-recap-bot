[pytest]
# Put the repo root on sys.path so tests can `import market_recap` when run as
# a bare `pytest` (the console script doesn't add the cwd the way `python -m
# pytest` does).
pythonpath = .
testpaths = tests
